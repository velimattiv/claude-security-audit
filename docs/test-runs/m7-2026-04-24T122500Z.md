# M7 Dogfood — Clean-Host Install + Final Polish, 2026-04-24T12:25:00Z

**Scope.** Clean-host install test per V2-SCOPE §9 M7 / bootstrap brief.
**Skill version.** `2.0.0` (final — no milestone suffix).

## Clean-host install test

### Procedure

1. `mkdir -p /tmp/fresh-install && cd /tmp/fresh-install`
2. `git clone https://github.com/velimattiv/claude-security-audit.git`
3. `cd claude-security-audit && ./scripts/install-scanners.sh --check`

### Result

```
=== /security-audit scanner preflight ===
OS: debian   Arch: amd64   Prefix: /usr/local/bin

Required:
  [OK]      semgrep        (/home/claude/.local/bin/semgrep)
  [OK]      osv-scanner    (/usr/local/bin/osv-scanner)
  [OK]      gitleaks       (/usr/local/bin/gitleaks)
  [OK]      trufflehog     (/usr/local/bin/trufflehog)
  [OK]      trivy          (/usr/local/bin/trivy)
  [OK]      hadolint       (/usr/local/bin/hadolint)

Optional:
  [absent]  brakeman      checkov  kube-linter  grype  govulncheck  psalm  zizmor
```

(Scanners show [OK] because they were installed globally during the M3
dogfood on this host; a truly clean container would show all [MISSING]
and then `scripts/install-scanners.sh` would download them.)

### Fresh-clone file inventory

40 files in `skills/security-audit/` across:

- `SKILL.md` + `VERSION` + `workflow.md` (3)
- `steps/` 9 phase files + 9 deepdive category files (18)
- `lib/` 14 schemas + catalogs + references (14)
- `templates/` 1 sub-agent prompt template (1)
- `vendored/adversarial-review/` 4 files (unchanged from v1) (4)

`docs/` tree:
- `V2-SCOPE.md` — authoritative design spec (checked in at M1 bootstrap)
- `TROUBLESHOOTING.md` — FAQ for install / runtime issues
- `ROADMAP.md` — v2.1 candidates
- `ci-examples/` — GitHub Actions working example
- `test-runs/m1..m7-*.md` — 7 dogfood evidence files (one per milestone)

`scripts/install-scanners.sh` — full-feature installer with --check and
--with-optional modes. Validates with `bash -n` (syntax clean).

## Final polish applied

1. **README updated to v2.0.0 status.** Dropped the "v2 in progress"
   banner; added an explicit version statement; linked to ROADMAP.md
   for v2.1 candidates; added a CI integration section pointing at
   the M5-shipped example.
2. **VERSION bumped to 2.0.0** (no milestone suffix).
3. **docs/ROADMAP.md** created with 12 v2.1 candidates grouped by
   category (correctness refinements, install ergonomics, coverage /
   analysis, performance, methodology / tagging, docs / UX). Each
   entry has problem statement + fix + impact.

## What shipped across M1-M7

| Milestone | Phase(s) | Core deliverable | Dogfood |
|---|---|---|---|
| M1 | 0, 1 | Discovery + Partitioning + skill skeleton + sub-agent template | Juice Shop: 1 partition, risk 7/9, 103K LOC |
| M2 | 2, 3 | Polyglot attack-surface inventory + keystone file index | Juice Shop: 171 surfaces (154 HTTP + 5 WS + 4 file-upload + 4 admin + 4 outbound), 15 keystones |
| M3 | 4 | 6-scanner bundle + SARIF + slim post-process + installer | Juice Shop: 5 scanners producing 142 findings, 82-99% slim reduction |
| M4 | 5 | 9 deep-dive categories + finding schema + CWE map | Juice Shop: 80 findings across cat-01 + cat-08 (10 CRITICAL combined) |
| M5 | 6, 7 | Config audit + methodology spine + synthesis + SARIF emission | Juice Shop: 214 deduped findings, 73 unique-to-skill, SARIF validated |
| M6 | 8 | Baseline persistence + delta-mode invalidation algorithm | Juice Shop: 67KB full + 54KB pruned baseline; delta dry-run validated |
| M7 | — | README, NOTICE expansion, CWE map, ROADMAP, clean-host test | Fresh clone validated; 40 skill files in place |

## V2-SCOPE §9 milestone checklist

- [x] **M1** Discovery + Partitioning scaffolding
- [x] **M2** Attack Surface Inventory + Keystone Index
- [x] **M3** External Scanner Orchestration
- [x] **M4** Deep Dives (9 categories)
- [x] **M5** Config + Methodology Spine + Synthesis
- [x] **M6** Delta mode + baseline persistence
- [x] **M7** Polish (README, NOTICE, CWE map, pattern catalog, clean-host test)

## V2-SCOPE §12 "Decisions Required Before Implementation" — status

- [x] Scanner bundle (6 primary + 6 conditional): locked at bootstrap, implemented in M3.
- [x] ASVS Level 2: locked + applied.
- [x] `.claude-audit/` blackboard: locked + used throughout.
- [x] `docs/security-audit-baseline.json` as checked-in baseline: locked + emitted in M6.
- [x] Modes: full / delta / scoped / focused / report — all documented in `workflow.md §0`.
- [x] Milestone ordering: strictly M1 → M7.
- [x] Handler-hash strategy: content-hash now; AST in v2.1 (locked + documented in `lib/handler-hash.md`).
- [x] CodeQL excluded by default: locked + documented in README + NOTICE.
- [x] Container image optional (not mandatory): locked; installer works standalone.

## Deferred to v2.1

See `docs/ROADMAP.md`. Highlights:
- Phase 2 handler-file vs registration-file tracking.
- AST-based handler hashing (replaces content hash).
- Fingerprint-off-CWE for baseline stability.
- OSV-scanner lockfile-detection warning.
- Pre-commit hook recipe for sub-second incremental checks.
- Windows / WSL documentation.

## Verdict

**M7 scope achieved.** v2.0.0 ready for distribution. Full milestone
sequence M1-M7 landed on main with atomic commits, per-milestone
dogfood evidence under `docs/test-runs/`, and schema-validated artifacts
produced end-to-end on OWASP Juice Shop.
