# Polyglot Dogfood — cat-04 MITM on gosec (Go), 2026-04-24T19:17:00Z

**Target.** `github.com/securego/gosec` @ `24ee992` (shallow clone,
/tmp/audit-targets/gosec, 248 Go files, ~45K LOC).
**Category exercised.** `cat-04-mitm.md` §"Go" section.
**Skill version.** `2.0.1-remediation`.
**Purpose.** Close the v2.0.0 adversarial-review finding #1 — "polyglot
claim is aspirational, not validated." Go grep patterns had zero
execution evidence before this run.

## Execution

Minimal Phase 0 pre-populated (primary language Go, single partition,
dev trust zone). One sub-agent spawned against the partition via the
template in `templates/subagent-prompt.md`, `model: opus`. Sub-agent
followed `steps/deepdive/cat-04-mitm.md` verbatim and validated its
output against `lib/finding-schema.json` before returning.

## Result

| Metric | Value |
|---|---|
| Findings | 20 |
| Severity | 0 CRITICAL · 2 HIGH · 2 MEDIUM · 13 LOW · 3 INFO |
| Sub-agent tokens | 79,359 |
| Wall time | 248 sec |
| Tool uses | 46 |

**Go grep patterns confirmed firing (from `cat-04-mitm.md §"Go"`):**

- `InsecureSkipVerify\s*:\s*true` — 11 hits across
  `autofix/openai.go`, `analyzer_test.go`, `testutils/g402_samples.go`,
  `README.md`.
- `tls\.Config\s*\{[^}]*InsecureSkipVerify\s*:\s*true` (multiline) —
  same set as above.
- TLS-version regex `TLSv1\.0|TLSv1\.1|SSLv[23]|SSLv3` — hit
  `cmd/tlsconfig/tls_version.go` and `cmd/tlsconfig/tlsconfig_test.go`.
- `tls.VersionTLS10` / `VersionTLS11` companion probe — hit
  `testutils/g402_samples.go:366`, `rules/tls.go:44-45`,
  `cmd/tlsconfig/tls_version.go`.
- `ssh.InsecureIgnoreHostKey()` — category invariant #3 extension, hit
  `testutils/g106_samples.go:15`. The sub-agent correctly treated SSH
  host-key disabling as MITM-adjacent.

## Top finding

`gosec:mitm:0001` — `autofix/openai.go:56` — `tls.Config{InsecureSkipVerify: true}` inside an OpenAI-API client, gated by an `-ai-skip-ssl` CLI flag, **CWE-295**, **HIGH** severity (runtime-toggleable).

Self-referential note: `rules/tls.go` IS gosec's own G402 detector —
the sub-agent's Go pattern would flag gosec's own detector source as a
finding. The sub-agent noted this and emitted it as an **INFO**-level
self-reference finding, not a false positive to suppress.

Remainder of the 20 findings are intentional G402/G106 test fixtures
(LOW) and codegen latent-risk cases (MEDIUM).

## Schema validation

```
$ python3 scripts/validate-findings.py \
    --schema skills/security-audit/lib/finding-schema.json \
    /tmp/audit-targets/gosec/.claude-audit/current/phase-05-mitm-gosec.jsonl
OK (jsonschema): 20 findings validated against finding-schema.json
```

All 20 rows conform to `finding-schema.json`, including CWE presence
and OWASP ID format.

## Verdict

**Go polyglot coverage validated.** `cat-04-mitm.md §"Go"` produces
real, correctly-classified findings on a real Go codebase.
